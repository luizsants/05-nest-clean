# === Variáveis específicas para testes (apenas o que é diferente de .env) ===
DATABASE_URL="postgresql://docker:docker@localhost:6000/nest-clean-test?schema=public"
AWS_BUCKET_NAME="ignite-nest-clean-test"